Конечно\! Вот обновленный `README.md`, адаптированный для деплоя на **Railway** и включающий важные примечания о временном хранении файлов.

-----

## 🚀 Go File Transfer (Быстрая передача файлов)

[](https://golang.org/)
[](https://developer.mozilla.org/en-US/docs/Web/HTML)
[](https://railway.app/)

-----

### 🌟 Обзор проекта

Простой и минималистичный веб-сервер, написанный на **Go**, предназначенный для **временной** передачи файлов между устройствами в сети (телефон \<-\> компьютер).

Использует чистый HTML/CSS для адаптивного и интуитивно понятного интерфейса.

### ⚠️ Важное примечание о хранении

Этот сервер использует **временное хранилище (Ephemeral Storage)**, что характерно для PaaS-платформ (Railway, Heroku).

> **Файлы, которые вы загружаете, могут быть удалены при перезапуске, обновлении или масштабировании сервера.**
> **Не используйте этот сервис для долгосрочного хранения\!** Он предназначен только для быстрой передачи файлов с немедленным скачиванием.

-----

### 🛠️ Требования

  * **Go** (версия 1.16 или выше)
  * Аккаунт на **Railway** (для облачного деплоя)
  * **Git**

-----

### ⚙️ Локальный запуск (Для тестирования)

Для тестирования в локальной сети (например, между вашим компьютером и телефоном, подключенным к тому же Wi-Fi):

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://www.nic.ru/info/blog/repository/
    cd file-transfer
    ```
2.  **Запустите сервер:**
    ```bash
    go run main.go
    ```
3.  **Доступ:** Откройте `http://localhost:8080`.
      * С телефона используйте **локальный IP-адрес** вашего компьютера (например, `http://192.168.1.5:8080`).

-----

### ☁️ Деплой на Railway (Для облачного доступа)

Railway автоматически определяет, что это Go-проект, и использует динамическое определение порта, которое реализовано в `main.go`.

#### 1\. Подготовка

Убедитесь, что ваш файл `main.go` обновлен для динамического порта, как указано ранее:

```go
port := os.Getenv("PORT")
if port == "" {
    port = "8080"
}
listenAddr := ":" + port
// ...
log.Fatal(http.ListenAndServe(listenAddr, nil))
```

#### 2\. Деплой

1.  **Подключите Railway к вашему репозиторию GitHub.**
2.  Создайте новый проект на Railway и выберите опцию **Deploy from GitHub repo**.
3.  Выберите ветку с вашим кодом.
4.  Railway автоматически соберет и запустит ваше приложение.

#### 3\. Использование

После успешного деплоя Railway предоставит вам **публичный домен (URL)**. Этот URL можно использовать с любого устройства в любой точке мира для обмена файлами.

-----

### 🛑 Остановить сервер

Чтобы остановить работу сервера (при локальном запуске), вернитесь в окно терминала, где он запущен, и нажмите **`Ctrl + C`**.

-----

### 📄 Лицензия

Этот проект распространяется под лицензией **MIT**. Подробнее см. файл [LICENSE].

-----