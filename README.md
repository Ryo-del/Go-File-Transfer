## 📂 Go File Transfer (Быстрая передача файлов)

[](https://golang.org/)
[](https://developer.mozilla.org/en-US/docs/Web/HTML)
[](https://www.google.com/search?q=LICENSE)

-----

### 🚀 Обзор проекта

Это минималистичный веб-сервер, написанный на **Go**, предназначенный для быстрой и временной передачи файлов между устройствами в локальной сети (например, с телефона на компьютер или наоборот).

Проект использует чистый HTML и CSS для создания интуитивно понятного и адаптивного пользовательского интерфейса.

### ✨ Ключевые особенности

  * **Минимализм:** Чистый, адаптивный дизайн, удобный для использования на мобильных устройствах.
  * **Простота:** Запуск одной командой Go.
  * **Высокая производительность:** Использует встроенный HTTP-сервер Go.
  * **Функциональность:** Поддерживает загрузку и скачивание файлов.

### ⚙️ Требования

  * [Go (версия 1.16 или выше)](https://golang.org/dl/)

### 🛠️ Установка и запуск

Следуйте этим шагам, чтобы запустить сервер на вашем устройстве:

#### 1\. Структура проекта

Создайте следующую структуру директорий:

```
file-transfer/
├── main.go               # Код сервера Go
├── uploads/              # Автоматически создается для хранения файлов
└── static/               # Статические файлы
    ├── index.html        # Интерфейс загрузки
    └── style.css         # Стилизация
```

#### 2\. Размещение кода

Убедитесь, что файлы (`main.go`, `index.html`, `style.css`) содержат код, предоставленный ранее.

#### 3\. Запуск сервера

Откройте терминал в корневой папке `file-transfer` и выполните:

```bash
go run main.go
```

Вы должны увидеть вывод:

```
Сервер запущен на http://localhost:8080
Загрузки будут сохранены в ./uploads
```

### 🌐 Использование

1.  **Доступ:** Откройте в браузере адрес, указанный в консоли (например, `http://localhost:8080`).
      * *Для доступа с других устройств в той же сети (например, с телефона), замените `localhost` на **IP-адрес** вашего компьютера (например, `http://192.168.1.5:8080`).*
2.  **Загрузка:** Нажмите **"Выбрать файл"** и выберите файл с вашего устройства. Затем нажмите **"Загрузить"**.
3.  **Скачивание:** После успешной загрузки вы будете перенаправлены на страницу с прямой ссылкой для скачивания этого файла. Используйте эту ссылку на другом устройстве.

### 🛑 Остановить сервер

Чтобы остановить работу сервера, вернитесь в окно терминала, где он запущен, и нажмите **`Ctrl + C`**.

-----
