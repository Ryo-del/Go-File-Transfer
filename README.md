
## 🚀 Go File Transfer (Быстрая передача файлов)

[](https://golang.org/)
[](https://developer.mozilla.org/en-US/docs/Web/HTML)
[](https://railway.app/)

-----

### ✨ Обзор проекта

Это минималистичный веб-сервер, написанный на **Go**, предназначенный для **временной** передачи файлов между устройствами в локальной или глобальной сети (телефон \<-\> компьютер).

Проект обеспечивает удобный интерфейс для загрузки и встроенный **менеджер файлов** для скачивания и удаления.

### ⚠️ Важное примечание о хранении

Сервер использует **временное хранилище (Ephemeral Storage)**, что характерно для облачных платформ (Railway, Heroku).

> **Файлы, которые вы загружаете, могут быть удалены при перезапуске, обновлении или масштабировании сервера.**
> **Проект предназначен только для быстрой, краткосрочной передачи данных.**

-----

### ⚙️ Требования

  * **Go** (версия 1.16 или выше)
  * Аккаунт на **Railway** (для облачного деплоя)
  * **Git**

### 🏗️ Структура проекта

```
file-transfer/
├── main.go               # Основной код сервера Go
├── go.mod                # Управление модулями и версией Go
├── go.sum                # Хеши зависимостей (для повторяемости сборки)
├── uploads/              # Директория для сохранения файлов
└── static/               # Статические файлы
    ├── index.html        # Интерфейс загрузки
    ├── manager.html      # Интерфейс менеджера файлов
    └── style.css         # Стилизация
```

-----

### 🛠️ Установка и запуск (Локально)

Для тестирования в локальной сети:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Ryo-del/Go-File-Transfer.git
    cd file-transfer
    ```
2.  **Инициализируйте модули:** Убедитесь, что файлы `go.mod` и `go.sum` созданы:
    ```bash
    go mod tidy
    ```
3.  **Запустите сервер:**
    ```bash
    go run main.go
    ```
4.  **Доступ:**
      * На компьютере: `http://localhost:8080`
      * С телефона: `http://[Ваш_локальный_IP]:8080` (например, `http://192.168.1.5:8080`)

-----

### ☁️ Деплой на Railway

Проект готов к автоматическому деплою на Railway благодаря динамическому определению порта в `main.go`.

1.  **Проверьте готовность:** Убедитесь, что файлы `go.mod`, `go.sum`, `main.go` и папка `static/` **закоммичены** и отправлены на GitHub.
2.  **Подключите Railway:** Создайте новый проект на Railway и выберите опцию **Deploy from GitHub repo**.
3.  **Автоматическая сборка:** Railway автоматически определит проект как Go, соберет его и запустит.
4.  **Использование:** Используйте публичный домен (URL), предоставленный Railway, для доступа к сервису с любого устройства.

-----

### 🗂️ Возможности Менеджера Файлов

После загрузки файла вы можете перейти в `/manager`, где:

  * **Скачивание:** Файлы доступны для скачивания по прямой ссылке.
  * **Удаление:** Используйте кнопку "🗑️ Удалить" для немедленного удаления файла с сервера.

-----

### 📄 Лицензия

Этот проект распространяется под лицензией **MIT**.

-----
